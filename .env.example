# SPARK CORE — five server-side secrets (never NEXT_PUBLIC_, never committed)
GITHUB_TOKEN=        # fine-grained PAT: Contents R/W on spark-core, Metadata read
GROQ_API_KEY=        # console.groq.com
ADMIN_PASSWORD=      # the only door into /forge
SESSION_SECRET=      # openssl rand -hex 32
EVOLVE_SECRET=       # openssl rand -hex 32
